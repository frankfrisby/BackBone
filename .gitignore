# ============================================
# BACKBONE Engine — .gitignore
# ============================================
# Code vs User Data boundary:
#   CODE (tracked):  src/, apps/, bin/, tools/, skills/, scripts/, docs/, packages/
#   USER DATA (ignored): lives at ~/.backbone/users/<uid>/
# ============================================

# ----- Dependencies -----
node_modules/

# ----- User Data (engine creates these at ~/.backbone, NOT here) -----
# These dirs may appear in the repo root during dev but are NOT code.
# Leading / anchors to repo root so src/data/, src/services/memory/ etc. are NOT caught.
/data/
/memory/
/projects/
/screenshots/
/_profiles/

# Generated artifacts accidentally written into source tree
src/data/*.json

# ----- Environment & Secrets -----
.env
.env.local
.env.*.local
*.pem
*.key
*.crt
credentials.json
tokens.json
*-token.json
*-config.json
!.env.example
!apps/web/.env.example

# ----- Claude Code / AI session state -----
# User-specific permissions and AI memory (not portable across machines)
.claude/settings.local.json
.claude/projects/*/memory/
.backbone-codex-last-message.txt

# ----- Build Outputs -----
dist/
build/
*.log

# ----- Web App -----
apps/web/out/
apps/web/node_modules/
apps/web/.next/

# ----- Firebase -----
.firebase/
dev/firebase/functions/node_modules/

# ----- IDE -----
.idea/
.vscode/
*.swp
*.swo
*~

# ----- OS -----
.DS_Store
Thumbs.db
desktop.ini
nul

# ----- Temporary -----
*.tmp
*.temp
.cache/

# ----- Test Coverage -----
coverage/

# ----- Stale config duplicates -----
mcp-config.json
